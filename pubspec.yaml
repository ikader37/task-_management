name: cli_task_management
description: A sample command-line application with basic argument parsing.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.2

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  collection: ^1.19.0

dev_dependencies:
  lints: ^6.0.0
  coverage: ^1.15.0
  test: ^1.26.0
